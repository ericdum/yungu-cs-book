# OpenBook 使用说明

## 概述

OpenBook 是一个静态网站，用于展示《云谷高中计算机讲义》的内容。网站采用左侧导航、右侧内容的布局，支持章节折叠展开，每个 section 作为独立页面。

## 目录结构

```
openbook/
├── index.html          # 主页面
├── styles.css          # 样式文件
├── app.js             # 应用逻辑（路由、导航、Markdown渲染）
├── start.sh           # 启动脚本
├── README.md          # 说明文档
└── data/              # 数据目录（所有内容文件）
    ├── navigation.json    # 导航数据（自动生成）
    ├── images/            # 图片资源目录
    └── [章节名]/          # 各章节目录
        └── [section名].md # 各section的Markdown文件
```

## 快速开始

### 1. 启动本地服务器

```bash
cd openbook
./start.sh
```

或者使用 Python：

```bash
cd openbook
python3 -m http.server 8000
```

### 2. 访问网站

在浏览器中打开：http://localhost:8000

## 重新生成内容

如果修改了 LaTeX 源文件，需要重新生成 Markdown 文件：

```bash
# 在项目根目录执行
python3 tools/latex_to_openbook.py elegantbook-cn.tex openbook/
```

这个命令会：
1. 解析主 LaTeX 文件（elegantbook-cn.tex）
2. 识别所有章节（chapter）和节（section）
3. 将每个 section 转换为独立的 Markdown 文件
4. 按照 `章节名/section名.md` 的结构组织文件
5. 将所有内容文件（包括 Markdown、图片、导航文件）放到 `data/` 目录
6. 生成 `data/navigation.json` 导航数据

## 功能特性

### 左侧导航栏
- 显示所有章节和 section
- 支持章节折叠/展开
- 当前阅读的 section 会高亮显示
- 点击 section 标题即可跳转到对应内容

### 右侧内容区域
- 自动渲染 Markdown 内容
- 支持代码高亮
- 支持表格、列表、引用等格式
- 响应式设计，适配不同屏幕尺寸

### 路由功能
- 支持浏览器前进/后退
- URL 中包含当前 section 路径（使用 hash）
- 可以直接通过 URL 链接到特定 section

## 技术实现

### 前端技术
- **HTML/CSS/JavaScript**：纯原生实现，无框架依赖
- **Marked.js**：Markdown 渲染库（通过 CDN 加载）
- **Fetch API**：异步加载 Markdown 文件

### 转换工具
- **Python 3**：LaTeX 到 Markdown 转换
- 支持 LaTeX 常用命令转换：
  - 章节标题（part, chapter, section）
  - 文本格式（textbf, textit, texttt）
  - 列表（enumerate, itemize）
  - 代码块（minted）
  - 提示框（tcolorbox）
  - 图片（includegraphics）

## 自定义样式

可以修改 `styles.css` 来自定义网站外观：
- 修改颜色主题
- 调整布局尺寸
- 更改字体样式

## 部署

### 静态托管

可以将整个 `openbook` 目录部署到任何静态网站托管服务：
- GitHub Pages
- Netlify
- Vercel
- 自己的 Web 服务器

### 注意事项

1. 确保所有文件使用 UTF-8 编码（支持中文）
2. 服务器需要正确设置 MIME 类型（.md 文件应作为 text/plain 或 text/markdown）
3. 如果使用 Nginx，可能需要配置：
   ```nginx
   location / {
       charset utf-8;
   }
   ```

## 故障排除

### 内容无法加载
- 检查浏览器控制台是否有错误
- 确认 Markdown 文件路径正确
- 检查文件编码是否为 UTF-8

### 导航不显示
- 确认 `data/navigation.json` 文件存在且格式正确
- 检查浏览器控制台是否有 JavaScript 错误

### 样式异常
- 清除浏览器缓存
- 检查 `styles.css` 是否正确加载

## 更新日志

- 2025-01-XX：初始版本
  - 支持章节导航
  - 支持 Markdown 渲染
  - 支持响应式布局

